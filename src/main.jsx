import React from "react";
import ReactDOM from "react-dom/client";
import InklineDashboard from "./InklinePricingDashboard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <InklineDashboard />
  </React.StrictMode>
);
