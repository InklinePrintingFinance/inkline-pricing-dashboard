# Inkline Pricing Dashboard

Vite + React app. Deployed on Vercel.

## Local dev
```
npm install
npm run dev
```

## Notes
- Pricing data (saved orders, settings) persists to the browser's `localStorage`, per device/browser. It is not synced across devices.
